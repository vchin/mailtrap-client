{
  "name": "mailtrap",
  "version": "1.0.0",
  "description": "simple mailtrap api",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "lint": "tslint src/**/*.ts",
    "build": "tsc",
    "test": "tsc && ava --verbose",
    "ci-test": "concurrently \"npm run mock-server\" \"ava --verbose\" -s last -k",
    "mock-server": "node dist/mock-server.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vchin/mailtrap"
  },
  "devDependencies": {
    "@types/chai": "^4.1.2",
    "@types/express": "^4.11.1",
    "@types/node": "^9.6.1",
    "ava": "^0.25.0",
    "chai": "^4.1.2",
    "concurrently": "^3.6.1",
    "express": "^4.16.3",
    "sinon": "^4.5.0",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "moment": "^2.21.0"
  },
  "ava": {
    "files": [
      "dist/**/*.test.js"
    ],
    "source": [
      "src/**/*.ts"
    ]
  }
}
